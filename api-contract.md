# API Contract

> **Not yet written.** There is no API surface. `InventoryErp.Web` is a Razor MVC application returning HTML views, not JSON — see `ui-flow.md` for its routes. This document applies only if a REST or minimal API is added.
